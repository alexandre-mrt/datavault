{
  "name": "datavault",
  "version": "0.1.0",
  "description": "Decentralized data marketplace on 0G. Buy and sell datasets for AI training, stored on 0G Storage.",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@0gfoundation/0g-ts-sdk": "latest",
    "ethers": "^6.13.0",
    "hono": "^4.6.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
